# Mapeo de Conversaciones — Prueba Técnica Mesa247

> **Entregable 3**: tus conversaciones con la IA. Este documento es el índice
> vivo de la conversación: qué decidimos, qué preguntaste, qué rechazamos y por
> qué. Se completa con los chats reales por parte (nota, backend, frontend)
> como anexo al entregar.

## Resumen ejecutivo

Comencé con la nota técnica ya redactada (corte de producto: aviso in-app en
vez de WhatsApp, short polling, sin drag & drop pesado) y el encargo del
diseñador como única fuente. A partir de ahí trabajamos así:

1. **Preguntas primero, código después**: definimos 4 forks de tecnología
   (BD local, polling del frontend, estilos, tests) antes de escribir una línea.
2. **Roadmap por tareas**: rechacé hacer todo de una — cada batch se aprueba,
   se implementa y se verifica de a uno (T1–T16, `docs/roadmap.md`).
3. **Documentación en el proyecto**: el decision log, el PRD, las convenciones
   y la arquitectura viven en `docs/` — nada queda solo en memoria del chat.
4. **Estado actual**: backend completo y verificado; frontend con T7, T8, T16 y **T9 (vista anfitrión)** y T10/T11 implementados con rediseño dark; el board del anfitrión pasó por el rediseño del U29/D51 (full-width tablet, cards + chips de estado, filtros y stats); faltan T12–T13 (verificación punta a punta, deploy, entrega) — `docs/roadmap.md`.

**Quién lleva el timón**: el candidato. Cada decisión de producto, tecnología
y convención la tomó la persona; la IA propuso y ejecutó según lo aprobado.

## El timón (intervenciones del candidato)

| # | Fecha | Qué dijo / decidió | Impacto |
|---|-------|--------------------|---------|
| U1 | 2026-09-14 | "No es necesario usar engram, con documentación en el proyecto está bien" | Persistencia en `docs/`, no en memoria externa |
| U2 | 2026-09-14 | "Quiero que me hagas preguntas en cuanto a elección de tecnologías" | 4 forks resueltos antes de codear (D8–D11) |
| U3 | 2026-09-14 | "SOLID para la implementación" + regla de comentarios | D7 y D13 (`docs/convenciones.md`) |
| U4 | 2026-09-14 | "Me gustaría dividir por task, si tenemos un roadmap sería mejor" | Roadmap T1–T13, trabajo de a uno |
| U5 | 2026-09-14 | "Revisemos error del editor al cargar python" / warning de `pydantic_settings` en el editor | Verificación de entorno; fix de intérprete en `.vscode/settings.json` |
| U6 | 2026-09-14 | "Vamos a incluir que esto está enfocado a mobile first; tablet/desktop no cubre" | PRD §4.3 + D17 (con corrección de matiz, ver U7) |
| U7 | 2026-09-14 | "Tablet sí está contemplada para el anfitrión" | Corrección: host responsive es requisito del brief (D17 final) |
| U8 | 2026-09-14 | "Arquitectura por capas, en contraste con limpia/hexagonal (enfocadas a equipos)" | D19 + `docs/arquitectura.md` + reestructura real del código |
| U9 | 2026-09-14 | "Mejorar el histórico de conversaciones con resumen y lo que yo comento" | Este documento |
| U10 | 2026-09-14 | "Vamos a incluir tanstack router, react hook form" al arrancar T6 | Stack del frontend definido: TS Router + RHF (D28) |
| U11 | 2026-09-14 | "usa pnpm no npm" | Package manager del frontend: pnpm (D28) |
| U12 | 2026-09-14 | "Incluir como coding standard: custom components, seguir SOLID, manejar el theme con tailwind, las validaciones con zod" | Convenciones de frontend documentadas + `zod` al stack (D29) |
| U13 | 2026-09-14 | Corrige la config del router plugin: API nueva `tanstackRouter({ target: 'react', autoCodeSplitting: true })` antes de `react()` | `apps/web/vite.config.ts` actualizado; build verde con code-splitting por ruta (chunk `routes-*.js`) |
| U14 | 2026-09-14 | "Hay que crear las variables para el theme de tailwindcss, puedes usar /impeccable para tener una idea" | Theme tokens en `@theme` (OKLCH, roles semánticos) — D30 |
| U15 | 2026-09-14 | "Hay que poner como pendiente la seguridad y acceso para la vista de anfitrión" | Corte documentado: T14 pendiente post-piloto (D31), PRD §4.2/§7 alineados |
| U16 | 2026-09-14 | "Hay que incluir base-ui, pnpm add @base-ui/react" | Primitivas headless al stack: **D32** (`@base-ui/react` 1.8.0) |
| U17 | 2026-09-15 | "He actualizado la nota técnica para incluir seguridad por PIN; hay que definir nuevas tareas en el roadmap" | PIN por local entra al alcance del piloto (T14–T16): reemplaza el corte D31 (D33) |
| U18 | 2026-09-15 | "Vamos por la task T7 (vista comensal — Unirse)" | T7 implementada: `/join/:slug` con form RHF+Zod, navegación al turno (D34) |
| U19 | 2026-09-15 | "Vamos a mejorar el input para el número porque por país el código puede variar, hay que incluir eso" | Teléfono con código de país, default según el slug (D35) |
| U20 | 2026-09-15 | "y no debe usar select ya debería mostrar el código como prefijo" | El código de país es **prefijo fijo** (addon de `InputGroup`), no select: el QR/slug del local ya define el país (D36) |
| U21 | 2026-09-15 | "Vamos a rediseñar las vistas de comensal (join + turno) con tema oscuro" vía /impeccable | Rediseño dark del flujo comensal: **tema base dark** para todo el producto (no un modo), canon de la categoría, barra de oficio fijada por 4 productos (D37) |
| U22 | 2026-09-15 | "Vamos por la task T8 … usar el local storage para que cuando el usuario cierre el navegador pueda volver a ver el estado … una vista … por cards. se puede limpiar una vez acepte o cancele, solo puede permanecer … 8 horas desde que se envía el ticket" | T8 completada: **persistencia local del turno**, vista "Tus turnos" por cards, TTL 8 h, cleanup al estado terminal (D38) |
| U23 | 2026-09-15 | "Continuemos con la task T16" | T16 completada: login tablet con PIN en el frontend + sesión JWT en localStorage + `Authorization` en `client.ts` + 401 devuelve al login (D41) |
| U24 | 2026-09-16 | "vamos a continuar con la task T9" | T9 completada: vista anfitrión real (cola en vivo, transiciones, priorizar, modal QR) en reemplazo del placeholder de T16 (D42–D44) |
| U25 | 2026-09-16 | "vamos a usar un input-odp … 6 digitos en total, actualiza el seed y model para que acepte digitos y caracteres" | PIN del host migra a **6 caracteres alfanuméricos** con componente OTP custom (`input-otp` 1.5.0): backend contract `^[a-zA-Z0-9]{6}$`, seed `AZUL24`/`VIENT4`/`MEDIT6` (D45) |
| U26 | 2026-09-16 | "vamos a mejorar el estilo del input otp debe abarcar todo el ancho y estar alineado con el button hay que incrementar el height" | OTP rediseñado a ancho completo: slots `h-12` con `flex-1`, separador `flex-none`, caret más alto (D46) |
| U27 | 2026-09-16 | "vamos a continuar con la task T10 del @docs/roadmap.md, pero vamos a incluir turbo repo" | Turborepo para unificar comandos + reporte del día (D47, T10) |
| U28 | 2026-09-16 | "vamos a por la task T11 del @docs/roadmap.md, incluye la forma de levantar el repo y la lista de comandos, technologies empleadas y el flujo con el agente en este caso gentle-ai y skills" | T11 completada: `README.md` raíz con quickstart 5 min (Turborepo), tabla de endpoints del contrato API, stack y flujo de trabajo con el agente — entregable 2 del brief (D49); de paso, el test de seed quedó alineado a los PINs numéricos actuales (`111333`/`222555`/`333666`), que el commit `0ae1372` cambió sin actualizar la suite |
| U29 | 2026-09-16 | "vamos a hacer mejoras en UX, debes usar la skill impeccable, primero en la vista del host no vamos a usar un container como card, va a ser una vista limpia enfocada a tablet que abarque todo el ancho con 24px de padding, incluir como titulo el nombre del restaurante y una descripcion, cola con pequeñas cards por cliente en una row, no eliminar cuando se atiende o cancela sino mostrar chip de estado, filtros por estado arriba, recuento del total de usuarios y espera media junto al filtro de estado" | Rediseño del board del anfitrión (D51): full-width tablet `px-6`, header con nombre + descripción del local (nuevo `GET /host/{slug}` + `description` en el modelo/seed), cards por cliente con chip de estado (los terminales siguen visibles), filtros por estado + stats "En cola" y "Espera media" |
| U30 | 2026-09-16 | "para el sign in tambien quitemos el container de la card y vamos a instalar esta libreria de icons pnpm add lucide-react y usar en los inputs y botones tambien podemos incluir en la card de clientes" | Login del host sin container de card (D52); **lucide-react** al stack y iconos en el input del PIN (D52), botones del login, header del board y cards de clientes (el candidato además ajustó la card a layout horizontal: nombre+personas, chip centrado, info de turno y acciones) |
| U31 | 2026-09-16 | "en el reporte del dia hay que corregir el total de se sentaron, se debe contar el total de personas por ejemplo si el usuario escribe 4 personas entonces debe contar 4 ahora solo cuenta el usuario que registra" | El reporte del día cuenta **personas, no grupos** (D53): `joined`, `seated`, `left_without_seat` y `no_show` suman `party_size`; `avg_wait_minutes` sigue siendo el promedio por grupo atendido |
| U32 | 2026-09-16 | "ahora hay que incluir cuando se hace cuanto tiempo se hizo la llamada por ejemplo: hace 1 min. este texto estara a la derecha del label de turno y los colores seran neutral, amarillo si llegan a los 7min y rojo si es mas de 10 min" / "usa dayjs como libreria" | El llamado muestra **"hace X min"** a la derecha de "Turno N" (D54) con semáforo de colores |
| U33 | 2026-09-16 | "hay que corregir timezone del estado no se actualiza se mantiene en hace <1 min" | Bug de timezone arreglado (D55): "hace X min" quedaba en "<1 min" porque el API serializaba `notified_at` naive-UTC sin offset |
| U34 | 2026-09-16 | "hay que incluir un middleware para tener en el server los logs de las peticiones" | Request logging middleware en el server (D58) |
| U35 | 2026-09-16 | "Vamos a mejorar la ux del guest para usar notificaciones del navegador cuando se llame, quitar container de cards, eliminar número al sentarse/cancelar y botones de navegación" | Implementación de notificaciones push del navegador, rediseño de vistas sin container, ocultar número en estados terminales y añadir botones de navegación en las vistas de join y ticket |
| U36 | 2026-09-16 | "Mostrar lista de tickets pendientes en la vista de unirse (/join/:slug) para cuando el usuario re-escanee el QR y haya tickets guardados" | Al re-escanear el QR, el usuario puede ver el estado de sus turnos guardados y decidir unirse a uno nuevo o continuar esperando |
| U38 | 2026-09-17 | "hay que hacer un fix para el reporte del dia, solo se deben contar los registros del dia no los previos" + "y crea un test para validar esto" | Reporte del día (y cola del host) cortan "hoy" en la medianoche **local** del país, no UTC (D61); test de regresión nuevo |
| U39 | 2026-09-17 | "vamos a incluir un NumberField component en el package del shared … usar el base-ui primitive … en las props incluye una que sea buttonVariant … corregir los estilos de los componentes y usar clsx y tailwind merge … crear un util en el folder de shared que se llame cn" | NumberField (Base UI) + util `cn` (clsx + tailwind-merge) al shared, refactor de merges de clases y migración de "Comensales" del join form (D62) |

## Partes del trabajo

1. **La nota técnica** — completada (`nota_tecnica_mesa247.md`)
2. **Backend** — FastAPI completo y verificado (T1–T5, T14–T15)
3. **Frontend** — React + TypeScript en `apps/web/` (T6–T9, T16; rediseño dark ver D37)

---

## Documentos del proyecto

| Documento | Ruta | Rol |
|-----------|------|-----|
| PRD | `docs/PRD.md` | Qué y para qué del producto (producto puro) |
| Nota técnica | `nota_tecnica_mesa247.md` | Entregable 1 — arquitectura, cortes, modelo de datos |
| Roadmap | `docs/roadmap.md` | Plan de trabajo por tareas |
| Convenciones | `docs/convenciones.md` | Reglas de código (comentarios, limpieza) |
| Arquitectura | `docs/arquitectura.md` | Capas y reglas de dependencia (D19) |
| Mapeo de conversaciones | `docs/mapeo-conversaciones.md` | Entregable 3 — decisiones, preguntas, rechazos |

## Registro de decisiones (Decision Log)

| # | Fecha | Parte | Decisión | Por qué | Alternativas rechazadas |
|---|-------|-------|----------|---------|------------------------|
| D1 | 2026-09-14 | Nota | Notificación **in-app** en vez de WhatsApp Business para el piloto | Las plantillas de Meta se rechazan y se cobran por país; el comensal ya tiene la web abierta viendo su puesto | WhatsApp API, SMS |
| D2 | 2026-09-14 | Nota | **Short polling** (5–8 s) en vez de WebSockets/SSE | Redes móviles inestables en la puerta; polling tolera cortes y reconexión; Cloud Run no favorece conexiones persistentes | WebSockets, SSE |
| D3 | 2026-09-14 | Nota | **Drag & drop simple**: botones de priorización en la tablet | Last-write-wins sobre `position_index`; dos anfitriones simultáneos | Reordenamiento complejo con DnD UI pesado |
| D4 | 2026-09-14 | Nota | Ruteo multi-tenant por **slug** (`/join/{slug}`, `/host/{slug}`) | Desacopla UI de IDs internos; escala a 150 locales | IDs directos en URL |
| D5 | 2026-09-14 | Nota | Backend **stateless**, estado solo en BD | Cloud Run escala a múltiples instancias sin estado en memoria | Estado en memoria, hilos Python |
| D6 | 2026-09-14 | Backend/Frontend | Stack: FastAPI + SQLModel + React + TypeScript (Vite) | Stack declarado por Mesa247 + velocidad de desarrollo | — |
| D7 | 2026-09-14 | Global | Principios **SOLID** en implementación | Decisión explícita del candidato | — |
| D8 | 2026-09-14 | Backend | **SQLite en local + MySQL por env var** (`DATABASE_URL`) | SQLModel abstrae el driver; README arranca en 5 min sin instalar MySQL, prod usa Cloud SQL | MySQL nativo local, solo SQLite |
| D9 | 2026-09-14 | Frontend | **TanStack Query** para el polling del puesto (refetchInterval 5 s, retry con backoff) | Caching/retry/refetch on focus gratis, robusto sobre redes malas | Hook propio `usePolling` |
| D10 | 2026-09-14 | Frontend | **Tailwind** para estilos | Iteración rápida sobre 5 pantallas, tokens consistentes | CSS puro + variables |
| D11 | 2026-09-14 | Tests | **pytest backend punta a punta** (TestClient): join, reindex, llamar/sentar/cancelar, concurrencia de posición | "Tests solo de lo que importa" — lo que se rompe es la API, no lo visual | vitest frontend |
| D12 | 2026-09-14 | Backend | Scaffold: FastAPI factory + config vía pydantic-settings + engine SQLModel sincrónico + `/health` con probe DB | Composición en `create_app()`, configuración centralizada, dev con SQLite | Config en módulos sueltos, async engine |
| D13 | 2026-09-14 | Global | Convención de comentarios: código limpio y autoexplicativo, comentarios solo para el POR QUÉ no obvio | Decisión explícita del candidato — el código se lee, no se explica | Comentarios descriptivos, docstrings por defecto |
| D14 | 2026-09-14 | Backend | Tablas vía `SQLModel.metadata.create_all` en el lifespan, seed idempotente por slug | README 5 min sin migraciones; para producción real se sumaría Alembic | Alembic desde el día 1 (prematuro para el piloto) |
| D15 | 2026-09-14 | Backend | `QueueStatus` como Enum (`WAITING, NOTIFIED, SEATED, CANCELLED, NO_SHOW`) | Contrato explícito del ciclo de vida del ticket; SQLite lo almacena como VARCHAR, MySQL como ENUM | Strings sueltos |
| D16 | 2026-09-14 | Producto | PRD creado (`docs/PRD.md`) para separar el QUÉ del CÓMO | La nota era arquitectura; faltaba la descripción de producto mapeada | Mezclar PRD dentro de la nota técnica |
| D17 | 2026-09-14 | Producto | Principio **mobile first**: comensal optimizado para celular; host responsive por exigencia del brief (tablet); sin experiencia dedicada tablet/desktop por ahora | El brief exige la tablet del anfitrión, así que "tablet no se cubre" no aplica igual a ambos lados | Declarar tablet/desktop fuera de alcance de forma absoluta |
| D18 | 2026-09-14 | Global | `AGENTS.md` creado con comandos, convenciones y gotchas del repo | Onboarding de futuras sesiones de IA sin repetir descubrimientos | Dejarlo solo en memoria/conversación |
| D19 | 2026-09-14 | Arquitectura | **Arquitectura por capas** (`docs/arquitectura.md`): `api → services → models → core`, dependencia descendente | Capas da dirección unidireccional + testabilidad con poca ceremonia; clean/hexagonal agrega puertos/adaptadores/casos de uso pensados para equipos grandes | Clean Architecture, Hexagonal |
| D20 | 2026-09-14 | Backend | Tiempo estimado = posición × promedio móvil de duración de sentados recientes (últimos 10), fallback 5 min/grupo por local configurado; recalculado en cada poll | regla del PRD §6, sin ceremonia | modelo de colas M/M/c, constante fija por local, predicción por hora del día |
| D21 | 2026-09-14 | Backend | Slug desconocido → 404 y match exacto; teléfono sin formato estricto (solo max_length 20); joins duplicados permitidos (sin dedup) | el piloto no impone topes (PRD §6) y el QR trae el slug correcto | match case-insensitive, validación con regex E.164, dedupe por teléfono |
| D22 | 2026-09-14 | Backend | Join no devuelve tiempo estimado; GET /tickets/{id} lo calcula en vivo | roadmap T3 explicita "ticket + posición" en join; el tiempo se recalcula con cada poll (D2) y no debe cachearse en el join | incluir estimated_minutes en el join response |
| D23 | 2026-09-14 | Backend | **Slug = marca + país** (ej. `casa-mediterranea-cl`), único global | un mismo restaurante puede operar en dos países (PE/CL/EC/CO); el slug único global hoy impediría la segunda sede | constraint compuesto (country_code, slug) + URL `/join/{country}/{slug}` — rompe el contrato de ruteo y complica el QR |
| D24 | 2026-09-14 | Backend | **Matriz de transiciones** vía `PATCH /tickets/{id}` con `action`: notify (WAITING→NOTIFIED, setea `notified_at`), seat (NOTIFIED→SEATED, setea `seated_at`, único camino), cancel y no_show desde cualquier estado activo; terminales (SEATED/CANCELLED/NO_SHOW) intocables → 409 | PRD §5.2 y §6 ("un ticket solo se sienta si antes fue llamado"); CANCELLED = "sin sentarse" (cancela el anfitrión), NO_SHOW = "no vinieron al llamado" (ya no voy del comensal o no-show del host) | transición por `status` directo (deja huecos de matriz), auto-cancel por timeout (rechazado por D1: decide el anfitrión) |
| D25 | 2026-09-14 | Backend | **Reporte del día = 5 números** (`joined`, `seated`, `left_without_seat`, `no_show`, `avg_wait_minutes`); el **pico de cola queda fuera del endpoint** | PRD §4.1 y roadmap T10/T12 ("los 5 números del prototipo"); el pico histórico exacto requiere un log de eventos que el esquema fijo (D14) no tiene — reconstruirlo sería inventar datos | incluir pico aproximado por max posición (inexacto tras reindexar/reordenar), log de auditoría de eventos (cambio de esquema + ceremonia, Fase 2) |
| D26 | 2026-09-14 | Backend | Reporte: "hoy" = **día UTC**; espera media = promedio join→seated (minutos) de los sentados del día; `seated_at`/`notified_at` respetan el patrón aware-UTC del modelo | el piloto no modela TZ por local (simplificación declarada en el código); D20 ya promedia `seated_at − created_at` | día por timezone del país (mapeo country_code→TZ), ventanas por hora local |
| D27 | 2026-09-14 | Tests | La prueba de concurrencia de la T5 **destapó una race real**: la asignación de posición era COUNT + INSERT sin atomicidad (10 joins simultáneos → solo 5 puestos únicos). Fix mínimo en `services/tickets.py`: `threading.Lock` en proceso alrededor de count+insert+commit | el piloto corre un solo proceso (uvicorn 1 worker); un índice global rompe porque los tickets terminales conservan posiciones históricas que colisionan con las reindexadas; el índice único parcial funciona en SQLite pero no es portable a MySQL (D8). El lock serializa solo la asignación de posición, no la cola; el comentario documenta que multi-instancia requiere mecanismo a nivel BD | índice único parcial (D8: MySQL no soporta partial indexes), UNIQUE global (colisiona con posiciones históricas), advisory lock de BD (ceremonia innecesaria en single-process) |
| D28 | 2026-09-14 | Frontend | T6 + adiciones: **TanStack Router** (file-based routing con plugin de Vite) + **React Hook Form** al stack declarado (D9–D10); package manager **pnpm**. Scaffold en `apps/web/`: cliente API tipado espejo de `api/schemas.py`, QueryClient con retry 3 + backoff exponencial (tope 30 s) y polling 5 s en los hooks `useTicketStatus`/`useHostQueue`, Tailwind **v4** (plugin oficial, sin config file) | U10/U11 explícitos; el ruteo por archivos se adapta a 5 pantallas y el RHF evita revalidar formularios a mano; pnpm es más rápido y estricto con el lockfile; Tailwind v4 es el estándar actual sin postcss config. API sin prefijo → el cliente usa `VITE_API_BASE_URL` (default `http://localhost:8000`) y CORS ya permite 5173; sin proxy para no chocar con las rutas del frontend | React Router/Next (más ceremonia), react-final-form (menos ecosistema), npm (descartado por U11), Tailwind v3 + postcss (obsoleto) |
| D29 | 2026-09-14 | Frontend | Convenciones de frontend (se suman a D13): **componentes custom** en `src/components/` (sin librerías UI), **SOLID** en componentes y hooks (SRP, OCP por props/composición, ISP en props, DIP hacia contratos tipados), **theme vía `@theme`** de Tailwind v4 (cero valores hardcodeados), **validaciones con Zod** + `zodResolver` con RHF. Se agrega `zod` + `@hookform/resolvers` al stack (`docs/convenciones.md`) | U12 explícito; coherencia con D28 (RHF) y D19 (capas: misma dirección de dependencia); zod es el estándar TS y tipa el schema | librería UI completa (shadcn/ui — ceremonia para 5 pantallas), CSS Modules/vanilla-extract (fragmentan el tema), Yup/joi (menos integración con TS) |
| D30 | 2026-09-14 | Frontend | **Theme tokens** en `@theme` (`apps/web/src/index.css`): roles semánticos en **OKLCH** — superficies (surface, surface-raised), texto (ink, ink-muted), bordes (line), acción (brand, brand-strong, brand-soft, on-brand), estados (success/warning/danger + variantes soft), `font-sans` de sistema (sin webfont) y `shadow-card`. Brand = **terracota** (hue 40) sobre neutros cálidos | U14; guía /impeccable (nuevas paletas en OKLCH, roles y no swatches, color = acción/estado no decoración); PRD §4.3 (comensal con datos móviles → nada de webfonts); parejas fg/bg chequeadas contra WCAG AA. El brand es **supuesto a validar con el diseñador** — cambiar el hue es una línea | azul SaaS genérico (sin significado para hospitalidad), monocromo puro (pierde jerarquía de estados), webfont (peso extra y FOUT en la puerta del local) |
| D31 | 2026-09-14 | Roadmap/Producto | **Seguridad de host pendiente (T14)**: la vista `/host/{slug}` queda **sin autenticación** en el piloto — acceso por slug (obscuridad, no seguridad); el corte queda registrado como tarea pendiente post-piloto en el roadmap | U15; el brief evalúa "qué se cortó y por qué" → el corte debe estar escrito; PRD §4.2 ya asumía sin login (tablets compartidas, confianza del local) — ahora el roadmap nombra la deuda | PIN por local (simple pero se comparte entre staff), enlace con expiración (sin gestión de usuarios), login completo (ceremonia para un piloto de 3 locales) |
| D32 | 2026-09-14 | Frontend | **Base UI** (`@base-ui/react` 1.8.0) al stack: primitivas *headless* (dialog, select, radio, etc.) con accesibilidad ARIA integrada y **cero estilos impuestos** — matiza D29 ("sin librerías UI"): sigue prohibido traer look de terceros, pero las primitivas de comportamiento entran; el estilo siempre lo definen componentes custom + tokens del `@theme` | U16 explícito; el modal QR del anfitrión (T9) y estados que cambian necesitan accesibilidad sin reinventar ARIA; encaja con la filosofía D29 (look propio) y con Tailwind v4 (sin runtime de estilos) | shadcn/ui (trae estilos + ceremonia de copiado, ya citado en D29), Radix UI (equivalente headless, pero el candidato eligió Base UI), MUI (estilos propios, rompería el tema) |
| D33 | 2026-09-15 | Roadmap/Backend | **Seguridad de host entra al piloto (reemplaza el corte D31)**: PIN por local + JWT de sesión corta → T14–T16. `pin_code` en `Restaurant` guardado como **hash** (nunca texto plano); `POST /host/{slug}/login` valida el PIN y emite el token; dependency `require_host` (Bearer JWT) protege `/host/*` **y `PATCH /tickets/{id}`** — cierra el vector "un comensal altera la cola" que la nota §5.1 exige; el token se comparte entre staff del local pero expira y solo opera sobre el slug autenticado | La nota técnica actualizó el alcance del MVP (U17); el PIN hasheado evita que una fuga de la BD exponga credenciales; sin gestión de usuarios (3 locales, tablets compartidas) | Sin auth (obscuridad por slug, corte original D31), enlace con expiración, login completo con usuarios, PIN en texto plano en BD |
| D34 | 2026-09-15 | Frontend | **T7 — Vista comensal "Unirse" implementada**: ruta `/join/:slug` (file-based, mobile-first) con formulario RHF + `zodResolver` — schema Zod v4 en scope de módulo que **espeja el contrato backend** `JoinRequest` (teléfono suelto por D21, `party_size` ≥ 1); submit llama `joinQueue()` y navega a `/tickets/$id`. Componentes custom extraídos por D29: `Field` (label+input+error, ARIA con `useId`) y `Button` (variants primary/secondary). Errores de API en estado local (los resolvers Zod limpian `setError('root')`). `/tickets/$id` queda como **placeholder mínimo** que T8 reemplaza con la vista viva (polling 5 s) | U18; PRD §4.1/§4.3 (mobile first, datos móviles); D29/D30 (componentes custom, tokens, Zod en el borde) | Form en la ruta sin componentes (rompe D29 al repetir markup 3×), validación estricta de teléfono en frontend (contradice D21), `setError('root')` para el error de red (fragile con zodResolver), construir T8 completa ahora (fuera del batch T7) |
| D35 | 2026-09-15 | Frontend | **Teléfono con código de país (U19)**: nuevo `PhoneField` (select nativo de código + input tel) en `/join/:slug`. Códigos del piloto `+51` (Perú) y `+56` (Chile) — **default derivado del slug** (D23: el último segmento es el país), fallback `+51` (mercado base). El payload concatena `código + número local` en `phone_number` (contrato D21 intacto, suelto y ≤20); el schema usa `phone_code` (enum) + `phone_local` (≤15) como split solo de UI | U19; un restaurante PE y uno CL necesitan prefijos distintos y el QR/slug ya codifica el país — no pedir el país aparte; lista mínima de los 2 países del piloto (nada especulativo) | prefijo fijo `+51` (rompe para CL), campo de texto libre del código (error-prone), catálogo completo de países (gold-plating fuera del piloto), separar país en otro campo del form (el slug ya lo trae) |
| D36 | 2026-09-15 | Frontend | **Código de país como prefijo fijo, sin select (U20)**: `PhoneField` pasa a un `InputGroup` (patrón shadcn adaptado al `@theme`, sin cva/cn) con el código (`+51`/`+56`) como addon no interactivo a la izquierda de un único input tel. El form ya no tiene `phone_code`: el schema es `phone` (≤15) y el submit antepone `countryCodeForSlug(slug)`; el usuario no elige país porque el QR de un local PE/CL ya lo define — un comensal extranjero entra con el prefijo del local (supuesto de piloto) | U20; el select de D35 añade una decisión que el slug ya resolvió; un solo input con prefijo visible reduce fricción y pasos (también elimina el `z.enum` y sus checks) | mantener el select (U19/D35), input libre con máscara automática del prefijo (gold-plating), seleccionar país desde un menú desplegable (misma objeción que el select) |
| D37 | 2026-09-15 | Frontend | **Rediseño dark del flujo comensal (U21, /impeccable)**: tema base **dark** para todo el producto (no un modo — decisión del usuario 2026-09-15), dirección **canon de la categoría** (estándar del rubro jugado recto, sin ironía): neutros cálidos oscuros (surface `oklch(0.22 0.012 60)`, hue 60–80 — nunca azul-gris), **un solo acento ámbar** (`brand`) gastado solo en la acción primaria y el momento NOTIFIED, sistema sans (sin webfont, PRD §4.3), tarjeta comensal/turno como **un solo objeto físico tipo boarding pass** (número de turno gigante 5.5rem, línea de corte punteada, footer "Fila virtual — Mesa247"), motion `ticket-warm` 0.7 s expo con `prefers-reduced-motion`; barra de oficio fijada por el usuario: **Toast POS, pase de abordaje de aerolínea, app de Starbucks, OpenTable/Resy** (commit de marca en PRODUCT.md). T7 se re-estiliza con tokens existentes sin tocar contrato; T8 se implementó en su parte central (vista viva del turno con polling 5 s, estados WAITING/NOTIFIED y terminales con copy real) — "Ya no voy" (cancelar) queda pendiente en el roadmap. Design tokens + reglas documentados en `apps/web/DESIGN.md` + sidecar `.impeccable/design.json` | U21; PRD §3 (noche, luces del local, comensal en la puerta con datos móviles) pide foco cálido legible; D30 (roles semánticos OKLCH) se reusa — mismo nombre de tokens, valores oscuros; D14/D19 (sin migraciones, capas) intactos; el finish reviewer de /impeccable devolvió `ship` | modo oscuro opt-in del SO (el usuario pidió tema base, no modos), dirección "Order Up" lanzada por el dado (cocina/expedite — quedó como alterna completa), mundo letterpress/imprenta y mesa de corte de film (competitivas — descartadas por el usuario), azul SaaS genérico (D30 ya lo rechazó), colores hardcodeados (viola D30), mantener el placeholder de T8 (el turno real es el corazón del producto) |
| D38 | 2026-09-15 | Frontend/Backend | **T8 completada + persistencia local del turno (U22)**: nuevo endpoint comensal `POST /tickets/{id}/no-show` (reusa la matriz de transiciones D24 y mapea el "Ya no voy" del comensal a **NO_SHOW**, no CANCELLED — el reporte D25 los distingue); el ticket se guarda en **localStorage** (`mesa247.tickets.v1`) al unirse y aparece en la nueva vista **"Tus turnos"** (cards vivas con polling 5 s) en `/`; expira a las **8 h desde el join** (TTL sobre la fecha local de guardado, aproximación del `created_at` del servidor que el contrato no expone), se **limpia al llegar a un estado terminal** (SEATED/CANCELLED/NO_SHOW) desde la vista del turno o desde las cards; salir confirma con **AlertDialog de Base UI** (D32); reabrir un id guardado nunca extiende el TTL | El comensal con datos móviles (PRD §4.3) puede cerrar la pestaña y volver a su turno sin re-escaneo; conocer el id del ticket equivale a poseerlo (UUID no adivinable, mismo modelo de confianza que `GET /tickets/{id}` en el piloto); el "ya no voy" como NO_SHOW mantiene el lenguaje del dominio (D24) y el conteo del reporte (D25) | cleanup solo al recargar la pestaña activa (pierde el estado al cerrar — exactamente lo que U22 pidió resolver), cookie/IndexedDB (ceremonia para una clave), TTL 24 h (un turno de noche no debe sobrevivir al día siguiente), guardar un snapshot de posición en el storage (mentiría: el backend es la verdad, las cards consultan en vivo), "Ya no voy" como CANCELLED al estilo host (rompe la semántica D24), botón de salir sin confirmación (fricción mínima que evita bajas accidentales del reporte) |
| D39 | 2026-09-15 | Backend | **T14 — Auth PIN implementado (D33)**: `pin_code_hash` (NOT NULL, `VARCHAR(120)` — ver desviación abajo) en `Restaurant` con **PBKDF2-HMAC-SHA256** (salt aleatoria 16 B por PIN, 100 000 iteraciones, formato `pbkdf2_sha256$iter$sal_hex$digest_hex`, verificación con `hmac.compare_digest` — tiempo constante); `POST /host/{slug}/login` valida el PIN → **JWT HS256** de sesión corta (payload `sub`=id + `slug` del local, `iat`/`exp`; secret `jwt_secret` y expiración `jwt_expire_minutes=60` en `core/config.py`, defaults dev), responses: 401 "PIN incorrecto", 404 local no encontrado, 422 PIN no numérico (schema 4–6 dígitos); seed carga PINs demo **1111/2222/3333** por local — el PIN en claro vive SOLO en `seed.py` (script de aprovisionamiento), a la BD va solo el hash; dep **PyJWT 2.14.0** añadida. **Desviación**: la nota técnica declaraba `pin_hash VARCHAR(100)` pero el formato hex del hash ocupa **118 chars** → columna de 120 (actualizado también en la nota). Rutas `/host/*` siguen abiertas: `require_host` es T15 | D33 ya decidió PIN hasheado + JWT corto; sin gestión de usuarios (3 locales, tablets compartidas); PBKDF2 con sal por PIN evita forzar todos los hashes en una pasada si la BD se filtra (los 4 dígitos de PIN se fuerzan trivial en claro — el hash no es la defensa contra fuerza bruta, los 100k iteraciones + sal sí la encarecen); PyJWT es el estándar FastAPI; `compare_digest` evita timing attack | bcrypt/argon2 (más deps nativas para el piloto; PBKDF2 de stdlib es suficiente con sal 16 B), python-jose (menos mantenido que PyJWT), JWT con `exp` como única reclamación (sin `slug`: T15 no podría scope-arlo al local), devolver el PIN o su hash en el login (nunca — el token solo lleva sub/slug), columna `VARCHAR(100)` de la nota (no alcanza — ver desviación) |
| D40 | 2026-09-15 | Backend | **T15 — `require_host` protege todas las rutas anfitrión**: dependency `require_host` (Bearer JWT vía `HTTPBearer(auto_error=False)`; decode en `services/auth.py` con PyJWT HS256 validando firma + exp + claims) devuelve `HostIdentity` (sub + slug); factory `require_host_for` scope-ada el token al `slug` de la ruta; `PATCH /tickets/{id}` exige que el ticket pertenezca a `sub` (`CrossRestaurantAccessError` en `apply_transition` → mapeada a 401, sin double lookup y sin que el router toque SQLAlchemy); **401 uniforme** para sin token / malformado / expirado / scheme no-Bearer / token de otro local (no 403: superficie de auth única, no filtra qué slugs existen y cumple el contrato del roadmap "401 sin/mal token"); `/host/{slug}/login` sigue público. Tests: `test_auth.py` (PIN incorrecto 401, login slug desconocido 404, 401 sin token en los 4 protegidos, tokens malformados/expirados/cruzados 401, token válido 200) y suites existentes autentican con `login`+`auth_headers` (PINS 1111/2222/3333) | D33 dejó `require_host` pendiente como T15; aislar JWT en services (no en routers) respeta D19; el guard en `apply_transition` hace el scope del ticket atómico con la transición | 403 para token cruzado (semántica "authed but forbidden" — se eligió 401 uniforme), decode del token dentro del router (viola D19), lookup del ticket en el router para comparar restaurant (toca SQLAlchemy directo, D19), middleware global con allowlist (ceremonia para 4 rutas) |
| D41 | 2026-09-15 | Frontend | **T16 — Login tablet (PIN)** completado: `/host/:slug` arranca con pantalla de PIN (RHF + Zod, contrato 4–6 dígitos espejo del schema Pydantic); el JWT se guarda en **localStorage** (`mesa247.host-session.v1`, un único slot con su slug y `expiresAt` derivado del `expires_in` del server, 60 min) y `client.ts` adjunta `Authorization: Bearer <token>` a **cada** request; un **401 que llegó con token adjunto limpia la sesión** y la ruta vuelve al login — el login público nunca adjunta el header, así un PIN incorrecto no borra una sesión viva de otro local; caducidad client-side como cortesía (el 401 del servidor sigue siendo la autoridad); `useHostSession` (subscriber del módulo de sesión, patrón de `ticketStorage` D38) re-renderiza la ruta al guardar/limpiar/expirar sin recargar; detrás del login, placeholder mínimo "Sesión iniciada" con "Cerrar sesión" — la cola en vivo es T9. PRD §4.1/§4.2/§5.2/§7.5 alineados (D33 ya había cambiado el alcance pero el PRD seguía diciendo "sin login") | T16 cierra la trilogía D33 (PIN hasheado → login JWT → protección en el frontend); "401 → vuelve al login" es el contrato literal del roadmap; una sola sesión por tablet = un local (staff compartido), misma filosofía de D8 (sin gestión de usuarios); verificado: `pnpm build` + `pnpm lint` verdes, ruta en el route tree, y smoke del contrato (401 PIN malo / 200 PIN bueno / 401 cola sin token / 200 con token) | estado de sesión solo en memoria (context) — se pierde al recargar y la tablet se apaga a mitad de turno (el token debe sobrevivir, mismo motivo que D38); cookie/IndexedDB (ceremonia para una clave); decodificar el JWT en el cliente para el exp (arrastra dep y el server es la autoridad — basta el `expires_in`); interceptar el 401 en cada hook/query (ceremonia duplicada — centralizado en `client.ts` cubre también T9); sesión por pestaña/slug (slot único documentado: una tablet = un local) |
| D42 | 2026-09-16 | Frontend | **T9 — Vista anfitrión implementada** en `/host/:slug`: `HostQueueView` (reemplaza el placeholder "Sesión iniciada" de T16) muestra la cola en vivo vía `useHostQueue` (polling 5 s, D2), con filas por turno (`QueueRow`): posición, nombre, comensales, ~tiempo esperado, estado (Esperando/Llamado ámbar) y acciones por estado — WAITING: **Priorizar + Cancelar + Llamar**; NOTIFIED: **Cancelar + Sentar**. Transiciones (`useTransitionTicket`) llaman `transitionTicket(id, action)` e **invalidan** `['host-queue', slug]` al terminar (reflejo inmediato sin esperar el poll; `onSettled` también invalida en error → refetch inofensivo). Fila en vuelo: deshabilitada + label "Llamando…/Sentando…/Cancelando…". Error por fila (`role="alert"`) con `ApiError.detail`. `Button` gana la variante **`danger`** (outline rojo con tokens existentes) para "Cancelar" — el anfitrión es quien cancela (D24); en filas no carga/vacío: skeleton (mismo patrón TicketCard), error con "Reintentar", vacío invita al QR. El header del panel agrupa "QR de la puerta" + "Cerrar sesión" (`clearHostSession`). Container del panel `max-w-md` (login conserva `max-w-sm`, comentario por-que en la ruta) | U24 retomó T9 tras T16 (el login ya estaba; el placeholder quedó listado como T9 en el roadmap); la invalidación de TanStack Query es el mecanismo canónico (D9) para reflejar el cambio de estado sin esperar 5 s; acciones explícitas por estado respetan la matriz D24 (un WAITING no se sienta, un NOTIFIED no se llama); fila deshabilitada durante la mutación evita doble-tap y races de 409 | transición → `setQueryData` con la respuesta (invalida la fuente; el shape de la respuesta no es la cola completa), invalidar solo en `onSuccess` (el refetch en error es barato y mantiene la cola al día), "Sentar" visible también en WAITING (violaría D24), confirm dialog para cada acción (ceremonia en la tablet de un local ocupado; solo "Ya no voy" del comensal confirma en D38 por ser autoservicio), propagar el 401 con banner (ya lo centraliza `client.ts` desde D41) |
| D43 | 2026-09-16 | Frontend | **Priorizar = subir UN puesto por tap** (no al tope, no drag & drop): `useReorderQueue` envía la permutación completa del orden activo a `reorderQueue(slug, order)` (LWW, contrato D3); el botón Priorizar está deshabilitado en `position === 1` y en filas en vuelo; la UI muestra "Priorizando…" en la fila tocada y la tabla refresca por invalidación (D42) | D3 decidió botones simples en la tablet (rechazó DnD pesado); subir de a un puesto es lo más predecible para dos anfitriones simultáneos (LWW) y respeta el sentido común de "priorizar" sin saltar a la cabecera de golpe (el anfitrión toca N veces si quiere); el 409 del contrato (solo permutaciones de ids activos) queda cubierto por el backend — la tablet resincroniza en el próximo poll | "Priorizar al tope" de un tap (salto brusco que el local no pidió), drag & drop (rechazado D3), dejar la priorización para T-post-piloto (es requisito explícito del mapa), botón en NOTIFIED (priorizar solo aplica a la espera) |
| D44 | 2026-09-16 | Frontend | **Modal "QR de la puerta"**: `DoorQrModal` con **Base UI Dialog** (D32, patrón del AlertDialog D38) que muestra un **`QRCodeSVG` (qrcode.react v4, agregado al stack)** codificando `` `${window.location.origin}/join/${slug}` `` dentro de una caja **blanca** (excepción documentada al tema dark: un QR oscuro sobre tablilla oscura no escanea) con la URL en texto debajo (para copiar/leer) y botón Cerrar | El QR es la entrada del comensal (PRD §4.1: el comensal escanea el QR en la puerta); el modal evita dejar el panel (la tablet queda en la cola); Base UI ya es el patrón de modales del producto (D32); qrcode.react v4 es SVG (no raster, sin peso de canvas) y `marginSize` reemplaza `includeMargin` del v3 | QR en el header siempre visible (ocupa espacio fijo del panel), navegación a una ruta `/host/:slug/qr` (el modal mantiene el contexto de la tabla), `react-qr-code`/`qrcode` raster (menos mantenidos / canvas pesado), caja con brillo del tema (`bg-surface`) — no escanea bien sobre dark |
| D45 | 2026-09-16 | Frontend/Backend | **PIN del host = 6 caracteres alfanuméricos + componente OTP** (U25): nuevo `InputOTP` custom (`input-otp` 1.5.0, adaptación del patrón shadcn/ui **sin `cn` ni lucide**, tokens del `@theme`: `border-line`, `ring-brand/50`, `border-danger`, carencia `caret-blink` nueva en el theme con motion guard); `HostLoginForm` migra de `Field`+`register` a `Controller`+`InputOTP` (3+3 slots con `InputOTPSeparator`, pattern `REGEXP_ONLY_DIGITS_AND_CHARS`), manteniendo label/error/aria en español; schema Zod + schema Pydantic alineados (`^[a-zA-Z0-9]{6}$`); seed con PINs memorables `AZUL24`/`VIENT4`/`MEDIT6`; tests backend actualizados al contrato (29 passed) | U25: el input OTP es el estándar para PINs (slots individuales con caret, mejor ergonomía en tablet que un input password); "dígitos y caracteres" amplía el espacio de PINs y aleja el PIN de fuerza-bruta trivial de 1111 (sin dejar de ser memorable en el seed); `input-otp` maneja paste + arrows + caret de forma accesible sin reinventar ARIA | input password nativo (se rompe el objetivo de separar en slots), 4–6 dígitos numéricos (U25 pide alfanumérico), `cn` utilty de shadcn (el repo no la tiene), separador con `lucide-react` (no es dependencia del proyecto), PIN "1111" (recién viable con 6 alfanuméricos siendo memorables) |
| D46 | 2026-09-16 | Frontend | **OTP a ancho completo y alto táctil** (U26): `InputOTP` container `w-full`; grupos y slots con `flex-1` (los 2 grupos reparten el ancho 3+3); slots `h-12` (48px — touch target de tablet, HIG mínimo 44px) con char `text-xl`; separador `flex-none px-1`; caret `h-5` | U26: el OTP debe abarcar el ancho del card igual que el botón "Entrar" (`w-full`) y tener más altura que los `size-8` iniciales para dedos en la tablet compartida del anfitrión; `flex-1` reparte los 6 slots parejo sin calcular anchos manuales | slots fijos `size-8`/`size-10` (no crecen con el card, desalineados del botón), un solo span centrado (pierde el formato de slots), separador grow (rompe la simetría 3+3) |
| D47 | 2026-09-16 | Frontend/tooling | **Turborepo** para orquestar comandos dev/build/test/lint/seed en monorepo | El piloto corre server+frontend, `turbo run dev` levanta ambos con un solo comando; `pnpm-workspace.yaml` + raíz `package.json` | Scripts manuales por carpeta, Nx, Lerna |
| D48 | 2026-09-16 | Frontend | **Reporte del día** como modal (Base UI Dialog) en la vista host, con los 5 números del prototipo | Vista simple de cierre: modal sobre la cola, sin cambiar la navegación; refetch manual, no polling (cierre es puntual) | Vista dedicada en ruta `/host/:slug/report`, sección inline en la cola |
| D49 | 2026-09-16 | Entrega | **T11 — README raíz + contrato API**: `README.md` con quickstart en 5 min (clone → `pnpm install` + `uv sync` → `pnpm seed` → `pnpm dev`), tabla de comandos Turbo/por-app, tabla de endpoints (9 rutas con auth, body y respuesta), contratos de estado/transiciones/errores, stack y sección "Cómo se construyó" documentando el flujo con el agente (orquestador + skills: fastapi, tanstack-*, react-hook-form, zod-4, impeccable, sdd-*) | El brief exige un README que levante el proyecto en 5 min como entregable 2; se eligió documentar también el flujo del agente porque los chats son el entregable 3 y el repo es la evidencia de cómo se trabajó; se verificaron los endpoints contra el código real (no inventar) | README en inglés (el repo y el brief son en español), documentar scripts de cada app por separado sin el camino Turbo (un comando es el punto), listar rutas "de memoria" |
| D50 | 2026-09-16 | Backend | **Seed desincronizado con la suite**: el commit `0ae1372` cambió los PINs demo del seed de alfanuméricos (`AZUL24`/`VIENT4`/`MEDIT6`) a numéricos (`111333`/`222555`/`333666`) sin actualizar `test_pilot_restaurants_are_seeded`, que fallaba `401`. Corregido en la T11: el test usa ahora los PINs del seed actual | Los tests deben reflejar el estado real del repo; la prueba de seed es la que valida los 3 locales demo | Cambiar el seed de vuelta a alfanuméricos (el contrato `^[a-zA-Z0-9]{6}$` admite ambos, pero el commit de hoy alineó el seed a numéricos: no revertir el último estado aprobado sin preguntar) |
| D51 | 2026-09-16 | Frontend/Backend | **Rediseño del board del anfitrión (U29, /impeccable)**: la vista `/host/:slug` pasa de `max-w-md` a **full-width** con `px-6 py-6` (el login conserva `max-w-sm` centrado) — **sin container card**; header con **nombre y descripción del restaurante** (nuevo `GET /host/{slug}` protegido por `require_host_for`, campo `description: str | None` en `Restaurant` + descripciones en el seed), acciones QR de la puerta / Reporte del día / Cerrar sesión arriba; toolbar con **filtros por estado** en pills (Todos / Esperando / Llamados / Sentados / Cancelados / No vinieron) + stats **"En cola: N"** (WAITING+NOTIFIED) y **"Espera media: ~M min"** (del reporte del día, polling ligero 30 s solo en el board — el modal D48 sigue con refetch manual); la cola pasa de filas a **cards en grid responsive** (`1/2/3/4 columnas`) con **chip de estado por estado** (Esperando neutro, Llamado `brand-soft/brand`, Sentado `success-soft/success`, Cancelado `danger-soft/danger`, No vino `warning-soft/warning`), activas `surface-raised` con acciones (Priorizar/Cancelar/Llamar, o Cancelar/Sentar), terminales receden (`surface`, sin acciones, "Fuera de la cola"); skeleton de grid y empty state por filtro. **Contrato backend**: `get_host_queue` devuelve **todos los turnos del día UTC** — vivos por `position_index` asc, luego terminales por `created_at` desc — para que un ticket atendido/cancelado **no desaparezca** y su chip tenga datos (antes el endpoint excluía terminales, D42). `prioritize` filtra a ids activos para seguir cumpliendo el contrato de permutación del reorden (D3). Suite actualizada al nuevo contrato (30 passed) | U29 explícito: "no vamos a usar un container como card", "vista limpia enfocada a tablet que abarque todo el ancho con 24px de padding", nombre + descripción como título, cards pequeñas por cliente, "no eliminar cuando se atiende o cancela sino chip de estado", filtros + recuento total y espera media junto al filtro; el estado terminal visible da contexto del día sin perder el foco en la cola viva; la espera media ya existe en el reporte (D25/D48) y reusarla evita una query nueva; "En cola" = gente esperando ahora (WAITING+NOTIFIED), el número operativo del anfitrión | mantener filas y solo ensanchar el container (ignora "sin container como card" y pierde la jerarquía por estado), ocultar terminales como en D42 (contradice "no eliminar cuando se atiende/cancela"), endpoint de info público sin auth (el board ya exige token, D33/D40), filtro en select nativo (las pills son el canon del tablero), espera media como query propia (duplicaría el reporte) |
| D52 | 2026-09-16 | Frontend | **Login sin container de card + iconos (U30)**: `HostLoginForm` pierde el `section rounded-2xl border bg-surface-raised shadow-card` (coherente con el board D51) — título/descripción/form sobre `bg-surface`, eyebrow "Mesa247" con icono `KeyRound`, CTA "Entrar" con `LogIn`; **`lucide-react` 1.46.0 al stack** e iconos en botones del header del board (`QrCode`, `ChartColumn`, `LogOut`), de las cards de clientes (Priorizar `ArrowUp`, Llamar `PhoneCall`, Sentar `Armchair`, Cancelar `X`); `Button` base gana `inline-flex items-center justify-center gap-2` para icono+texto sin prop nueva (retrocompatible: texto solo queda idéntico); el candidato además ajustó `QueueCard` a **layout horizontal** (nombre+personas a la izquierda, chip centrado, info de turno, acciones a la derecha) — se preserva tal cual | U30 explícito (quitar container del sign in, instalar lucide-react, iconos en inputs y botones + cards de clientes); lucide es el estándar de iconos del ecosistema React (SVG, tree-shakeable con Vite) y el tema ya viene en `currentColor`, así los icons heredan las rampas del `@theme` sin tokens nuevos; `inline-flex` en Button es el soporte mínimo para iconos y no cambia visual de los botones de texto existentes | mantener el card en el login (rompe la coherencia con U29/D51), `react-icons` (carga toda la librería, sin tree-shaking fino), íconos inline como SVG por componente (duplica markup, D29), prop `icon` nueva en Button (ceremonia: children alcanza) |
| D53 | 2026-09-16 | Backend | **El reporte del día cuenta personas, no grupos (U31)**: `joined`, `seated`, `left_without_seat` y `no_show` suman `party_size` de los turnos del día (un grupo de 4 sentado suma 4; el reporte se lee en comensales, la unidad de capacidad del local); `avg_wait_minutes` permanece como promedio por grupo atendido. Test `test_day_report_counts_the_pilot_numbers` actualizado a la semántica de personas (3 joins de 2 → joined 6, seated 2, left 2) | U31 explícito ("si el usuario escribe 4 personas entonces debe contar 4") + confirmación del usuario de **todos los conteos en personas** (pregunta de unidades: las opciones eran solo-seated vs. todo en personas; eligió todo en personas); unidades mezcladas en un mismo reporte confunden ("llegaron 3, se sentaron 2" — ¿2 qué?); el stat del board "En cola" (D51) sigue en grupos, es estado vivo de la cola y no parte de este pedido | contar solo `seated` en personas (deja el reporte con unidades mezcladas), contar grupos en todos lados (ignora U31), ponderar `avg_wait_minutes` por personas (la espera es por grupo, no por comensal) |
| D54 | 2026-09-16 | Frontend | **"hace X min" en las cards Llamado (U32)**: cuando el ticket está `NOTIFIED`, al lado de "Turno N" (donde iba la estimación) se muestra el tiempo transcurrido desde la llamada con **semáforo**: neutral (`text-ink-muted`) < 7 min, amarillo (`text-warning`) desde 7 min, rojo (`text-danger`) cuando supera 10 min; formato "hace 1 min" / "hace <1 min"; **`dayjs` al stack** para el diff de tiempo (`dayjs(now).diff(dayjs(notified_at), 'minute')`) y hook nuevo `useNow` (tick de 30 s) que refresca el reloj sin depender del poll del board (5 s) | U32 explícito con los umbrales y la elección de librería ("usa dayjs"); `notified_at` ya existía end-to-end (modelo lo setea al notificar D24, schema y tipo frontend lo exponían), así que el cambio es 100 % frontend; `useNow` con tick de 30 s es suficiente para granularidad de minutos y evita re-render por card en cada poll; dayjs es el estándar ligero del ecosistema (2 kB) y la petición del usuario lo fija; el `title` con el timestamp completo da el dato exacto sin ruido visual | calcular con `Date.now()` puro (el usuario pidió dayjs), colores hardcodeados fuera del `@theme` (rompe D29: se usa `text-warning`/`text-danger`), mantener la estimación además del "hace X min" (duplica info: el llamado ya venció la espera estimada), refresh solo con el poll de 5 s (deja el reloj congelado entre polls mientras el tablero no cambia datos) |
| D55 | 2026-09-16 | Backend/Frontend | **Timestamps del API con instante explícito (bug U33)**: SQLite guarda datetime naive-UTC (D26) y el API serializaba `notified_at` sin offset (`"2026-09-16T20:06:40"`) → dayjs en el navegador lo interpretaba como hora **local** (Perú UTC-5) y el diff quedaba negativo ("hace <1 min" eterno). Fix: `HostQueueItem` serializa `notified_at` con `field_serializer` que pinna `+00:00` cuando el valor es naive; el frontend parsea con plugin `dayjs/plugin/utc` (`dayjs.utc(...)`, robusto a naive y a offset). Test nuevo `test_host_queue_notified_at_is_utc_aware` | el contrato self-describing evita que cada consumidor futuro re-descubra la convención naive-UTC (exactamente el bug que U33 destapó); el serializer en el schema es el único punto que gobierna el wire (lo heredan `/host/{slug}/queue` y `/reorder`); `dayjs.utc` cubre ambos formatos mientras haya datos/servidores viejos en el ciclo | dejar el frontend parseando naive como local (el bug persiste), cambiar el modelo a DateTime(timezone=True) solo en MySQL (SQLite lo pierde igual), fix solo visual ("hace X min" con offset hardcodeado — miente sobre el instante) |
| D56 | 2026-09-16 | Frontend/tooling | **Separación host/comensal en dos apps Vite + package compartido (T17)**: `apps/web` → `apps/web-guest` (comensal: `/`, `/join/:slug`, `/tickets/:id`; dev en :5173) + `apps/web-host` nuevo (anfitrión: `/host/:slug`; dev en :5174). Superficie común movida a `packages/shared` (`@mesa247/shared`, consumido como fuente TS sin build): cliente API con factory **`createApiClient({ getToken?, onUnauthorized? })`** — el guest NO inyecta token y su bundle no contiene código de sesión del host (el 401 limpia sesión solo si la petición llevaba token, comportamiento D41 preservado); `types.ts` espejo del contrato; componentes UI (Button/Field/InputGroup/InputOTP/PhoneField); `queryClient` (retry/backoff D1); `styles/tokens.css` = **source única del `@theme` + base** (D30), importada por ambas apps. Backend: solo CORS suma `:5174` (config default + `.env.example`); **lógica intacta**. `git mv` preserva historial del guest; root scripts `dev:web-guest`/`dev:host`. Verificado: build + lint verdes en ambas apps, **bundle del guest sin símbolos de host** (grep), pytest 31 passed | U34; **el localStorage se comparte por ORIGEN**, no por app: `mesa247.tickets.v1` (guest) y `mesa247.host-session.v1` (host) solo quedan aislados si las apps se sirven en orígenes distintos — la topología de producción no está definida aún, así que las apps se diseñan independientes y el aislamiento lo decide el deploy, no el código (funciona para subdominios y también para mismo origen con paths, donde solo separa bundles). El enrutado del frontend **no es seguridad** — la real ya vive en `require_host` (T15/D40); el split aporta defensa en profundidad: el bundle del host (board, JWT, reporte) nunca llega al navegador del comensal, y el guest baja de peso. Dividir sin paquete compartido duplicaría cliente/types/tema y divergiría con el tiempo | mantener `apps/web` única con claves ya namespaced (el "problema de localStorage" es topología de producción, no un bug actual — se aceptó el split como mejora de entrega), duplicar cliente/tema en cada app (drift garantizado, viola el espíritu de D29), colocar el tema/base en cada app y solo compartir types (los tokens seguirían siendo fuente doble), `packages/shared` como librería con build+dist (ceremonia de release para un piloto: se consume la fuente directo) |
| D57 | 2026-09-16 | Backend | **Request logging middleware** (U35): `RequestLoggingMiddleware` en `core/logging.py` (stdlib, cero deps nuevas) — una línea INFO por request: método, path (**sin query string**), status HTTP, duración en ms y client IP; **nunca loguea cuerpos** (teléfonos/PIN son PII). Registrado en `create_app()` con `app.add_middleware`; logging configurado en el composition root (`main.py`) con `basicConfig`, porque uvicorn solo configura handlers para loggers `uvicorn.*` y sin eso `mesa247.http` no imprime (verificado en runtime); status `499` si el cliente corta antes de la respuesta. Verificado: uvicorn real imprime las líneas (200/404/405) y pytest 31 passed | U35; trazabilidad mínima de peticiones sin dep nueva; el `basicConfig` en la raíz de composición es el patrón de entrada de app (no en la "librería" `core/logging.py`); los cuerpos fuera del log porque el payload del join lleva teléfono y el login lleva PIN — una fuga de logs no debe exponer PII | structlog/loguru (deps nuevas y ceremonia de schema para un piloto), reusar el access log de uvicorn (formato fijo, sin duración del handler propio y no existe fuera de uvicorn), loguear body + query string (PII), middleware por endpoint (duplica el trabajo del global) |
| D59 | 2026-09-16 | Backend/Frontend | **Nombre del restaurante en el join comensal (U37)**: nuevo `GET /join/{slug}` **público** devuelve `RestaurantInfoResponse` (slug/name/description — mismo shape que `GET /host/{slug}` autenticado, sin token, 404 "Local no encontrado"); el comensal que escanea el QR ve el nombre del local como eyebrow (reemplaza el "Mesa247" genérico) y su descripción como sub-línea (si el local no tiene descripción o la info falla, cae al copy genérico y el form sigue usable — la info es progresiva, no bloquea el join). Frontend: `getRestaurantInfo` en `api/guest.ts` + hook `useRestaurantInfo` (`useQuery` con `staleTime: Infinity`: el nombre no cambia en la vida del turno, sin refetch) | El QR codifica `/join/{slug}` y es la primera superficie del comensal: confirmar el local antes de dar el teléfono (PRD §4.1), mismo valor que el header del board D51; reusar `RestaurantInfoResponse` aprovecha un shape ya probado por el host sin schema nuevo; público porque el join no tiene ni debe tener auth | duplicar name/description en el `POST /join/{slug}` response (contamina el contrato de join con datos que el join no consume), exponer `GET /host/{slug}` sin auth (rompe D40), hardcodear el nombre en el frontend (diverge del seed) |
| D60 | 2026-09-16 | Entrega | **Entregable 4 como markdown**: nuevo `entregable-4.md` en la raíz (junto a `nota_tecnica_mesa247.md`) con el formato que pide el brief ("diez líneas sobre algo que construí — qué corté, qué salió mal, qué haría distinto") y el código al centro: renderizado de `TicketCard` con records (`statusIcons`/`statusCopies` — el "antes" con bloques `status === 'X'` del historial, el "después" verbatim) y mejoras visuales de las cards del host (`STATUS_CHIP` record + card, chip de estado, terminales visibles "Fuera de la cola", verticales→cards). "Qué salió mal" documenta dos bugs reales: terminales excluidos del endpoint de la cola (D42→D51) y timestamps naive-UTC (U33/D55). Se enlaza desde el README (mapa de archivos) | El brief define entregable 4 como texto breve de autoevaluación con el criterio en "qué cortaste / qué salió mal / qué harías distinto"; el candidato pidió incluir el código como markdown; los entregables 1 (`nota_tecnica_mesa247.md`) y 3 (`docs/mapeo-conversaciones.md`) ya viven como docs en el repo, así que el 4 se materializa igual y se indexa en el README | meter el entregable 4 dentro de la nota técnica (D16 separa producto de arquitectura; el 4 es autoevaluación de ejecución), un doc solo de código sin la narrativa del brief (pierde lo que el brief mide: corte, error, aprendizaje), crear la carpeta `docs/` extra para un único archivo de entrega |
| D61 | 2026-09-17 | Backend | **Bug reporte del día: el corte "hoy" pasa de día UTC a día local del país** (U38): `get_day_report` y `get_host_queue` usaban medianoche UTC (D26), así que un registro hecho anoche a las 19:00–23:59 hora Lima (UTC−5) ya caía en el día UTC de HOY y aparecía en el reporte/cola del día siguiente. Fix: helper `_local_day_bounds(restaurant)` calcula el rango del día en `zoneinfo` del país (`country_code` → IANA: PE→America/Lima, CL→America/Santiago, fallback UTC) y lo proyecta a naive-UTC para el query (la DB sigue en UTC, D55). Test nuevo `test_day_report_counts_only_today_local_entries` con reloj fijo (monkeypatch de `datetime`) que demuestra el bug: 23:30 Lima == 04:30 UTC del día siguiente debe quedar fuera, 09:00 Lima == 14:00 UTC dentro. Verificado: el test falla con el corte UTC (joined=4) y pasa con el local (joined=2) | U38: "no se deben contar los registros previos, hay que hacer un filtro por fecha" — el filtro por fecha existía pero cortaba en la medianoche equivocada desde la perspectiva del local (D26 documentó la simplificación UTC con esta alternativa rechazada, y la operación real la destapó: el propietario hizo un join anoche y lo vio "hoy" en la app); el country_code ya existe en el modelo y el seed (PE/CL) así que el mapeo no agrega schema nuevo | mantener el corte UTC (el bug persiste y el reporte miente sobre el día), hardcodear "America/Lima" global (el seed ya tiene un local CL — el mapeo por país es lo correcto y no cuesta nada), día por creación en una TZ fija del servidor (depende del entorno de deploy, no del local) |
| D62 | 2026-09-17 | Frontend | **NumberField + util `cn` e integración en el join (U39)**: nuevo `NumberField` en `@mesa247/shared` compuesto con la primitiva **Base UI** (`NumberField as NumberFieldPrimitive` — Root→Group→Decrement/Input/Increment, alias pedido por U39): `label`/`error`/`id` con ARIA (`aria-invalid`/`aria-describedby` van en la parte **Input**, no en Root que es div), `value/min/max/step/onValueChange` pasan al Root (su `id` apunta al input interno); steppers con `Minus`/`Plus` de lucide y **`buttonVariant`** reutilizando el vocabulario de `Button` (`'primary'|'secondary'|'danger'`, default `secondary`; `ButtonVariant` pasa a exportado). Util **`cn`** (`packages/shared/src/lib/cn.ts`, clsx + tailwind-merge verbatim del candidato) y refactor de los merges de clases de `Button`/`InputGroup`/`InputOTP` de `.join(' ')`/template literals a `cn` (Field/PhoneField sin merges — diff mínimo); deps nuevas en shared: `clsx ^2.1.1` + `tailwind-merge ^3.7.0`. JoinForm: "Comensales" migra de `Field type="number"`+`register` a **`Controller` + `NumberField`** (`value`/`onValueChange`, `min 1`, `step 1`; schema zod y payload intactos — `z.coerce.number().int().min(1)` sigue validando: campo vacío → null → 0 → error). `.gitignore` suma `*.tsbuildinfo` (artefacto del build). Verificado: `pnpm build` verde en web-guest y web-host | U39; Base UI ya está en el stack (D32) y su NumberField cubre estado/step/ARIA que el `<input type="number">` nativo no; D45 había rechazado el util `cn` "porque el repo no la tiene" — ahora la tiene, así que el merge de clases se unifica (twMerge resuelve conflictos con `className` del consumidor); el anillo de foco del Group usa `focus-within` (el grupo no es focuseable; verificado en los `.d.ts` instalados) y los iconos stepper llevan `aria-label` (WCAG 4.1.2) | `<input type="number">` nativo (D34: sin steppers consistentes, UX pobre en tablet), managed component propio sin primitiva (reinventa estado/ARIA que Base UI ya da — contradice D32), `buttonVariant` con vocabulario nuevo (diverge del sistema de variantes existente), mantener `.join(' ')`/template literals (no resuelven conflictos de clases), refactor cosmético de Field/PhoneField sin merges reales |
| D63 | 2026-09-17 | Entrega/Infra | **Dockerfiles + compose para desplegar el piloto con un comando**: `Dockerfile.server` (python:3.12-slim + uv 0.12.13 desde `ghcr.io/astral-sh/uv`, `uv sync --frozen --no-dev` con lockfile primero y código después para cachear la capa de deps; runtime single-stage con seed opt-in), `Dockerfile.web-guest`/`Dockerfile.web-host` (node:24-alpine + pnpm 11.7.0 pinneado (`npm i -g pnpm@11.7.0`, evita corepack), `pnpm install --frozen-lockfile` en el workspace y `pnpm --filter <app> build` con `ARG VITE_API_BASE_URL` build-time; runtime **nginx:stable-alpine** sirviendo `dist` + `nginx.conf` compartido con fallback SPA (`try_files … /index.html`, necesario para `/join/:slug` y `/host/:slug`) y cache inmutable `expires 1y` para assets hasheados), más `.dockerignore` y `docker-compose.yml` con los 3 servicios en los **mismos puertos del dev** (8000/5173/5174 → CORS default de `core/config.py` y `VITE_API_BASE_URL` default intactos, cero knobs), SQLite en volumen nombrado (`DATABASE_URL` default `sqlite:////data/mesa247.db`), seed idempotente al arrancar (`python -m app.seed && exec uvicorn …` — la demo levanta con los 3 locales), healthcheck `GET /health` y web con `depends_on: service_healthy`. Verificado: `docker compose config` válido y build de las 3 imágenes (server + las dos web sirviendo el `dist` en nginx) | El brief evalúa la entrega (T13 = deploy) y el usuario pidió "despliegue sencillo": un solo comando que levante todo con los orígenes/puertos del dev para no tocar CORS ni recompilar el cliente; nginx es el patrón estándar para SPA (vite preview exige node en runtime, sin headers de cache y no es un server de producción) y el fallback cubre las rutas del router; `VITE_API_BASE_URL` es build-time porque Vite embebe `import.meta.env` (inyección runtime exigiría reestructurar el cliente) | `vite preview` como runtime web (levanta pero es un server de dev), un Dockerfile único para ambas web con `--build-arg`s (dos archivos igual de cortos y autocontenidos, cachean por capa igual), servicio MySQL por defecto en el compose (D8 lo soporta por env pero el cero-config del piloto es SQLite; queda como knob `DATABASE_URL`), puertos 8080/8081 (rompen CORS sin override), variable runtime para el API base (gold-plating fuera del piloto) |

## Preguntas de tecnología al candidato (resueltas)

- [x] Base de datos local → **SQLite + MySQL por env** (D8)
- [x] Obtención de datos en frontend → **TanStack Query** (D9)
- [x] Estilos → **Tailwind** (D10)
- [x] Alcance de tests → **Backend pytest punta a punta** (D11)

## Rechazos (qué le dijimos que no a la IA)

- **No a hacer todo de una**: la IA propuso avanzar de corrido; se eligió
  roadmap por tareas con OK explícito en cada una (U4).
- **No a la persistencia externa**: la IA sugirió guardar en memoria del
  sistema (engram); el candidato pidió documentación dentro del proyecto (U1).
- **No a "tablet/desktop no se cubre" en seco**: la IA (propuesta inicial)
  declaró tablet fuera de alcance; el candidato corrigió que la tablet del
  anfitrión **sí** está contemplada (U6, U7 → D17).
- **No a comentarios descriptivos por defecto**: se impuso la regla de que el
  código se explique solo y los comentarios solo cuenten el porqué no obvio (D13).
- **No a guardar el PRD dentro de la nota técnica**: se separó producto
  (QUÉ) de arquitectura (CÓMO) (D16).
- **No a la ceremonia de clean/hexagonal**: se eligió capas por ser un corte
  chico, de un solo dev (U8 → D19).

## Supuestos

- **Entorno del evaluador**: Python 3.12+, uv y Node 20+ (verificado en local:
  Python 3.12.3, uv 0.12, Node 24). El README asume uv para levantar todo.
- **Editor**: VS Code con intérprete `apps/server/.venv/bin/python` para evitar
  el warning de `pydantic_settings` (`.vscode/settings.json`).
- **Locales del piloto**: seed de 3 restaurantes con slugs estables
  (la-terraza-azul-pe, cuatro-vientos-pe, casa-mediterranea-cl) — datos en
  `apps/server/app/seed.py`.
- **D58**: Guest UX batch: navegador notifications, sin container de cards, ocultar número en terminales, botones navegación, tickets pendientes en join.
- **Supuestos de producto** (sin respuesta del diseñador): ver `docs/PRD.md §7`.
| D64 | 2026-09-17 | Frontend | **NotFound compartido sin container card (U40)**: nuevo `NotFoundPage` en `@mesa247/shared` (`packages/shared/src/components/NotFoundPage.tsx`, exportado desde el index) — título "Página no encontrada" + eyebrow "404" + descripción + acción opcional, centrado a pantalla completa sobre `bg-surface` con tokens del `@theme`, **sin card/container** (coherente con U29/U30/D51/D58); se conecta como `notFoundComponent` en `createRootRoute` de **ambas apps** (patrón idiomatico de TanStack Router v1, sin ruta archivo ni regenerar `routeTree.gen.ts`): guest con link "Volver al inicio" (`Link to="/"` estilizado con tokens), host solo texto porque no hay home válido (su única ruta es `/host/:slug`). Verificado: `pnpm --filter web-guest|web-host build` verde y lint sin errores nuevos | U40: "crear algo simple con texto y título, evitar usar un card container"; el sistema visual ya es texto sobre `bg-surface` sin cards fuera del flujo de ticket (D51/D52/D58), así que el 404 sigue el mismo lenguaje; un componente compartido evita duplicar markup/tokens entre apps (D56) | componente 404 duplicado en cada app (viola D56), card container con `surface-raised`/`shadow-card` (contradice la línea U29/U30/D51/D58), catch-all `$route.tsx` como 404 manual (`notFoundComponent` es el mecanismo nativo y cubre rutas no matcheadas de una), sin 404 (el SPA en nginx ya cae al fallback de D63 y muestra la home ante un path inválido) |
| D65 | 2026-09-17 | Frontend | **El join valida que el local exista antes de mostrar el form (U41)**: `useRestaurantInfo` sube del `JoinForm` a la ruta `/join/$slug`; la ruta ramifica sobre el query — `isPending` → "Cargando el local…" (centrado, sin card), `isError` con `ApiError.status === 404` ("Local no encontrado") → **`NotFoundPage`** + link "Volver al inicio", error no-404 → `NotFoundPage` con copy de conexión (no decirle al comensal que el local no existe cuando el servidor está caído). `JoinForm` ahora recibe **`restaurantName: string`** como prop (único uso que hacía del query, el eyebrow del local). Verificado: `pnpm --filter web-guest build` verde, lint sin errores nuevos, contrato del API (404 slug inválido / 200 slug del seed) | U41: "hay que validar en el join que exista el restaurante sino que muestre el empty" — antes cualquier slug pintaba el form (el GET ya respondía 404 pero el error del query se ignoraba y el eyebrow caía a "Mesa247"); la ruta, no el form, decide qué renderiza la página (el form ya nunca debe pintarse sin local confirmado); el hook ya existía con `staleTime: Infinity` (D37) así que no hay fetch nuevo | duplicar `useRestaurantInfo` en ruta y form (redundancia: el form solo consumía `name`), mostrar `notFoundComponent` vía `throw notFound()` (pondría copy genérico "Página no encontrada" y complica el control granular del mensaje), mostrar el form igual y que falle el POST (es el bug U41), tratar todo error como "local no existe" (miente en fallas de red) |