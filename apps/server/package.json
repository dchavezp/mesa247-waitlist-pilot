{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --port 8000",
    "test": "uv run pytest",
    "lint": "uv run flake8 .",
    "seed": "uv run python -m app.seed",
    "clean": "rm -f mesa247.db"
  }
}