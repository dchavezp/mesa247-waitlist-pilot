{
  "name": "mesa247-waitlist-pilot",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:server": "turbo run dev --filter server",
    "dev:web-guest": "turbo run dev --filter web-guest",
    "dev:host": "turbo run dev --filter web-host",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "seed": "turbo run seed",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}